# 更新日志 (CHANGELOG)

- **v1.19.4**:
  - **稳定性修复**: 将日志解析、临时文件清理、配置保存和 Cookie 失效通知中的 bare `except` 替换为明确的异常处理范围，避免吞掉系统级中断，并保持单个目标失败不影响其他目标。
  - **代码质量**: 使用 Ruff 检查并修复 E722 lint 问题，统一异常处理代码风格。

- **v1.19.3**:
  - **稳定性修复**: 429 限流等待期间释放 HTTP 请求信号量，避免多个限流请求占满并发槽位。
  - **稳定性修复**: 监控周期失败时正确执行指数退避，连续成功后恢复正常轮询频率。
  - **生命周期修复**: 配置迁移保存任务在插件关闭时等待完成，超时后才取消，避免迁移结果静默丢失。
  - **资源保护**: 推送队列增加最大积压上限，关闭时记录未处理消息数量，并完善队列任务完成标记。
  - **数据恢复**: 订阅配置为空或全部无效时自动从有效备份恢复；部分有效配置保留给用户修复。

- **v1.19.2**:
  - **重要修复**: 订阅分组页面保存现在会等待 AstrBot 配置实际落盘，保存失败会明确提示，不再提前显示保存成功。
  - **数据保护**: 订阅分组、热搜/每日总结勾选项和监控博主会同步原子备份到 `monitor_data.json`；首次升级会为现有配置补建快照，后续更新若框架配置被重置为空将自动恢复，避免重复配置。
  - **界面调整**: 推送统计移动到会话订阅分组和保存按钮之后，保存操作与对应分组配置保持相邻。

- **v1.19.1**:
  - **新功能**: 订阅分组管理页面新增近 7 天推送统计，可按日期查看 24 小时时段分布及博主推送排行。
  - **统计口径**: 每日推送日志改为在正文至少成功发送到一个会话后记录实际推送时间；首次初始化的历史微博标记为快照，不计入页面统计。
  - **界面优化**: "添加博主"输入框与按钮改为稳定的自适应表单布局，避免按钮在桌面宽度下被挤压；窄屏时自动改为上下排列。

- **v1.19.0**:
  - **正式发布**: 发布订阅分组管理页面、按会话独立配置热搜和每日总结、页面内维护监控博主，以及包含冒号会话 ID 的兼容修复。
  - **界面优化**: "添加会话"按钮移动至会话订阅分组配置框内，操作入口与配置内容保持一致。

- **v1.19.0-alpha-6**:
  - **修复**: 正确解析包含冒号的 AstrBot 会话 ID（如微信私聊 `weixin_personal_...:FriendMessage:...`），保存订阅分组后不再被误判为无效 UID，也能正常参与推送路由。

- **v1.19.0-alpha-5**:
  - **交互优化**: "仅指定博主"由原生多选下拉框改为逐项勾选列表，无需按住 Ctrl / Command 即可选择多个博主。

- **v1.19.0-alpha-4**:
  - **默认配置**: 默认开启运行日志、每日推送记录、每日发送总结和微博热搜监控。
  - **交互优化**: 将隐藏的 `weibo_urls` 融合进订阅分组 Page；现在可在"监控博主"区域添加、移除并保存微博 UID 或主页链接，无需再到原生插件配置页维护。

- **v1.19.0-alpha-3**:
  - **文案**: 订阅分组页面将"全部微博、热搜和总结"改为"全部微博博主"，明确热搜和每日总结由独立勾选项控制。

- **v1.19.0-alpha-2**:
  - **修复**: 为隐藏的 `subscription_delivery_options` 对象补齐必需的 `items` schema，修复 AstrBot v4.26.5 加载插件时的 `KeyError: 'items'`。

- **v1.19.0-alpha-1**:
  - **新功能**: 新增"订阅分组管理"Plugin Page，可用表格管理会话与微博账号的订阅关系；保存时仍使用原有 `subscription_mappings` 数据格式，导入导出和推送路由保持兼容。
  - **新功能**: 热搜与每日总结可按会话独立勾选接收范围；旧版 `*` 会话默认保持同时接收两类消息。
  - **交互优化**: 已监控的微博 UID 改为多选下拉列表；已不在监控列表中的旧订阅会保留显示，便于修复。
  - **校验**: 页面保存会拒绝空会话 ID、重复会话、`*` 与指定 UID 混用及无效 UID/微博链接；旧异常配置会明确提示，继续保存前需要确认移除。
  - **配置优化**: 原有扁平配置改为账号认证、监控频率、推送内容、媒体、过滤、日志总结、热搜等分类卡片；首次启动自动迁移旧值，保留隐藏兼容字段以防止升级时丢失配置。
  - **引导**: 原 `subscription_mappings` 配置位置保留说明，提示使用独立页面进行配置；`/weibo_umo` 同步更新引导文案。

- **v1.18.4**:
  - **新配置项**: `enable_image_download`（默认开启），控制是否下载并推送微博图片。关闭后只推送文字和视频，不下载图片。
  - **优化**: 重新组织插件配置项顺序，图片/视频/临时文件相关配置集中排列，方便查找。

- **v1.18.3**:
  - **修复**: `/weibo_check` 和 `/weibo_check_all` 命令在 `subscription_mappings` 未配置当前会话时静默不推送的问题。移除了 `if uid_targets:` 守卫，改为依赖 `_send_new_posts` 内部的 `fallback_target` 兜底，确保当前会话总能收到推送。

- **v1.18.2**:
  - **优化**: 临时媒体文件（图片/视频）保留时长改为可配置，默认 10 分钟（原 1 小时）。新增 `temp_media_retention_minutes` 配置项，设为 0 则不清理。
  - **重构**: 将 `_cleanup_temp_images` 重命名为 `_cleanup_temp_media`，清理间隔跟随配置值动态调整。

- **v1.18.1**:
  - **新配置项**: `video_download_timeout`（默认 60 秒）和 `video_send_timeout`（默认 60 秒），可配置的视频下载和上传超时。设置为 0 则不限制。
  - **推送队列**: 后台监控发现的新微博不再直接发送，而是加入 `asyncio.Queue` 队列，由独立消费者逐条处理。监控周期与推送过程完全解耦，大视频下载不会阻塞检查节奏。

- **v1.17.0**:
  - **新功能**: 统一推送目标配置。将 `target_conversation_id`（全局广播）和 `subscription_mappings`（订阅过滤）合并为单一的 `subscription_mappings` 配置项。使用 `*` 表示全局广播（该会话接收所有博主推送 + 热搜 + 每日总结），使用具体 UID 表示只接收指定博主。若只写会话 ID 而省略冒号（如 `群A`），自动补全为 `*`。
  - **迁移**: 首次启动时同步自动将旧版 `target_conversation_id` 中的全局目标迁移到 `subscription_mappings`（追加 `会话ID: *`），确保 run_monitor 启动前完成。
  - **重构**: 新增 `_iter_mappings()` 统一解析 subscription_mappings，消除三处重复解析逻辑。`_get_targets_for_uid` 路由逻辑大幅简化，不再需要"全局广播排除已订阅会话"的复杂判断。
  - **样式**: 移除所有配置项 description 的 `【全局】`/`【分组】` 前缀。
  - **破坏性变更**: `target_conversation_id` 已废弃。现有配置会自动迁移，新配置请直接使用 `subscription_mappings`。

- **v1.16.4**:
  - **修复**: 飞书平台微博推送时文字丢失的问题。将文字与图片拆分为两条独立消息发送，绕过 AstrBot 飞书适配器处理图文混合消息链时的列表引用 bug。所有平台（QQ、KOOK、飞书等）统一采用此方式，文字消息在前、图片消息在后。

- **v1.16.3**:
  - **修复**: `/weibo_cookie` 更换的 Cookie 现在会同时写入插件持久化数据文件，框架配置保存接口不可用时也能保证下次重载或重启后保留最新 Cookie。
  - **修复**: `/weibo_import` 导入包含 `weibo_cookie` 的配置时，同步写入持久化数据文件，避免导入后重启丢失。
  - **优化**: 插件启动时如果框架配置中的 Cookie 为空，会自动尝试从持久化数据中恢复上次保存的 Cookie。

- **v1.16.2**:
  - **新功能**: 新增 `/weibo_get` 命令，支持抓取并推送指定链接的单条微博。用法: `/weibo_get <微博链接>`。支持 `weibo.com` 和 `m.weibo.cn` 的多种链接格式。
  - **新功能**: 所有微博推送（包括定时监控、手动检查、`/weibo_get`）现在会自动获取并附带微博原图。图片以最高清质量下载，与正文合并在同一条消息中推送。支持 QQ、KOOK 等平台的图文混合消息。
  - **新功能**: 新增 `max_images_per_post` 配置项，允许用户限制每条微博最多获取的图片数量（默认 0，即获取所有图片）。
  - **优化**: 引入临时图片缓存机制，下载的图片自动存储在数据目录的 `temp_images` 子目录中，并每小时自动清理超过 1 小时的缓存文件，避免磁盘空间浪费。
  - **已知问题**: 飞书（Lark）平台图片无法正常显示。根因是 AstrBot 飞书适配器的 `_convert_to_lark` 方法在处理图文混合消息链时存在 Python 列表引用 bug（`ret.append(_stage)` 后 `_stage.clear()` 会清空已推入的内容），导致 post 消息中产生空段落。QQ 和 KOOK 平台不受影响。

- **v1.15.0**:
  - **新功能**: 新增会话订阅分组功能。支持通过 WebUI 配置 `subscription_mappings`，为每个会话独立指定需要接收哪些博主的微博推送。未配置订阅的会话保持原有行为（接收全部推送）。
  - **新配置项**: `subscription_mappings`（【分组】）: 会话订阅映射列表，格式 `会话ID: uid或链接,uid或链接`。每行配置一个会话的订阅关系。
  - **优化**: 所有配置项 description 均已添加 `【全局】` / `【分组】` 前缀标签，WebUI 中作用范围一目了然。
  - **优化**: `/weibo_status` 现在会显示订阅分组的开启状态和已配置的会话数量。
  - **优化**: 微博推送路由逻辑重构为按 UID 分发，`/weibo_check` 和 `/weibo_check_all` 命令同样遵循订阅分组规则。
  - **修复**: 修复仅配置 `subscription_mappings` 而未配置 `target_conversation_id` 时监控周期被跳过的问题。现在纯订阅分组场景可正常工作。Cookie 失效通知也会覆盖订阅分组的会话。

- **v1.14.3**:
  - **修复**: 修复 `/weibo_cookie` 命令因框架自动按空格分割参数导致 Cookie 字符串被截断的问题。现在从原始消息中提取完整参数，不再依赖框架的自动解析。
  - **修复**: 同步修复 `/weibo_import` 命令的相同问题，避免配置字符串因空格被截断。

- **v1.14.2**:
  - **变更**: 将 `/get_umo` 命令重命名为 `/weibo_umo`，避免与其他插件的命令冲突。功能保持完全一致。

- **v1.14.1**:
  - **移除**: 彻底移除相似度去重功能（v1.14.0 实验功能），包括 `enable_similarity_dedup`、`similarity_threshold`、`similarity_cache_size` 配置项以及所有相关算法和缓存逻辑。该功能在实际使用中表现不佳，已回滚。
  - **新配置项**: 新增 `hotsearch_show_link`，支持用户选择是否在热搜榜中显示每条热搜的微博搜索链接。默认开启（`true`），关闭后每条热搜仅显示序号和标题，界面更简洁。
  - **优化**: 插件重载时若 30 分钟内已推送过热搜，将自动跳过首次推送，避免频繁重载导致热搜消息刷屏（v1.13.1 引入，v1.14.1 保留并确认逻辑正确性）。

- **v1.14.0**:
  - **新功能（实验）**: 新增相似度去重功能。开启后，自动检测并过滤与近期推送内容高度相似的微博，避免重复推送。采用 SimHash 指纹快速过滤 + Jaccard 相似度精准验证的两级策略，在保证准确性的同时保持低开销。该功能为实验性质，未来可能根据反馈调整算法和参数。
  - **新配置项（实验）**: `enable_similarity_dedup`: 启用相似度去重（默认关闭）；`similarity_threshold`: 相似度阈值，范围 0.0~1.0（默认 0.7）；`similarity_cache_size`: 每个用户的去重缓存条数（默认 20）。
  - **优化**: `/weibo_status` 现在会显示相似度去重的开启状态及参数。
  - **优化**: 相似度缓存持久化到 `similarity_cache.json`，重启插件后不丢失。

- **v1.13.1**:
  - **优化**: 插件启动/重载时自动检查最近一次热搜推送时间。若过去 30 分钟内已推送过热搜，将跳过首次推送，避免因频繁重载插件导致热搜消息刷屏。
  - **优化**: 热搜推送完成后将推送时间持久化至数据文件，确保重启后仍可正确判断。

- **v1.13.0**:
  - **新功能**: 新增 `/weibo_cookie` 命令，支持直接通过命令更换微博 Cookie。用法: `/weibo_cookie <Cookie字符串>`。更换后自动验证 Cookie 有效性并尝试重载插件，无需手动进入 WebUI 操作。

- **v1.12.6**:
  - **修复**: 修复热搜链接在飞书等平台中因 `#` 字符（`%23`）导致 URL 被截断的问题。现在使用 `urllib.parse.quote` 对关键词进行标准 URL 编码，不再包裹 `#` 符号。

- **v1.12.5**:
  - **优化**: 优化微博热搜的推送展示格式，移除热度数值和前三名的 emoji 图标，统一为数字编号格式。
  - **优化**: 为每条热搜下方增加对应的微博搜索跳转链接，方便直接点击查看。

- **v1.12.4**:
  - **优化**: 为微博热搜接口增加携带 Cookie 的兜底策略。当无 Cookie 请求遭遇风控拦截时，会自动尝试使用配置好的 `weibo_cookie` 获取数据，提升热搜监控的成功率和稳定性。

- **v1.12.3**:
  - **修复**: 修复了微博热搜接口因未携带 Cookie 被拦截重定向至 visitor 页面导致无法获取热搜的问题。已切换至全新免 Cookie 接口，完全恢复"免 Cookie 使用"的特性。

- **v1.12.2**:
  - **新功能**: 热搜推送记录接入每日日志系统 (`logs/YYYYMMDD.log`)。开启 `enable_daily_log` 后，每次热搜推送都会以 `type: "hotsearch"` 格式记录。
  - **新功能**: 每日总结现在会统计当天的热搜推送次数（显示 `🔥 热搜推送：N 次`）。
  - **优化**: 重构每日总结的微博动态展示格式，分类显示「微博动态」和「热搜推送」。
  - **文档**: README 新增「热搜监控」专题章节，包含热搜消息格式说明、自定义变量和实际推送效果示例。
  - **文档**: README 补充热搜日志记录说明。

- **v1.12.1**:
  - **修复**: 修复热搜监控会抓取第一条强制性置顶条目的问题。现在通过 `itemid` 中的 `realpos` 字段精确过滤，只保留真实排名热搜。感谢用户反馈。

- **v1.12.0**:
  - **新功能**: 新增微博热搜监控功能。支持定时推送热搜榜前 N 条，间隔和数量均可配置。无需 Cookie 即可使用。
  - **新配置项**: `enable_hotsearch`: 开启微博热搜监控 (默认关闭)；`hotsearch_interval`: 热搜推送间隔（分钟），默认 60 分钟；`hotsearch_top_n`: 推送热搜前 N 条，默认 10 条；`hotsearch_filter_ads`: 过滤热搜广告位，默认开启；`hotsearch_message_format`: 热搜推送消息格式，支持自定义。
  - **新指令**: `/weibo_hot`: 手动查询当前微博热搜榜。
  - **优化**: `/weibo_status` 现在会显示热搜监控状态。

- **v1.11.3**:
  - **修复**: 修复 `cookie_notification_target` 配置解析的 bug，确保即使在配置值为列表或包含空格等情况下，也能正确解析并向指定目标发送通知。

- **v1.11.2**:
  - **新功能**: 增加 `cookie_notification_target` 配置项。允许用户指定 Cookie 失效时的通知目标，支持将通知发送到特定的账号或群聊，而不必发送到所有监控推送目标。
  - **优化**: 完善 Cookie 失效提醒机制。未配置 `cookie_notification_target` 时保持原样（向所有目标推送）；配置后则仅向指定目标推送，保护隐私并减少干扰。

- **v1.11.1**:
  - **新功能**: 增加 Cookie 健康检查功能。在每次抓取内容前检测 Cookie 是否有效，若失效则向用户发送一次提醒消息，避免因 Cookie 失效导致静默抓取失败。
  - **优化**: 完善 Cookie 失效提醒逻辑，同一失效周期内仅提醒一次，避免刷屏。

- **v1.11.0**:
  - **新功能**: 初始化监控时，自动将获取到的历史微博记录到每日日志（需开启 `enable_daily_log`），避免历史微博丢失。
  - **新功能**: 微博发布时间现在会从微博数据中解析并精准保存到日志中，不再统一使用记录时间。
  - **优化**: 完善微博时间解析逻辑，支持"刚刚"、"x分钟前"、"x小时前"、"昨天"、"MM-DD"及标准格式等多种微博时间显示方式。
  - **优化**: 增强日志记录去重逻辑，防止初始化或重复检查时在日志中产生重复记录。

- **v1.10.3**:
  - **新功能**: 增加 `/weibo_status` 命令，查看当前监控状态（监控账号数、推送目标、检查间隔等）。
  - **新功能**: 增加 `/weibo_summary` 命令，手动触发昨日总结推送。
  - **优化**: 手动触发获取的微博不计入每日推送日志。

- **v1.10.2**:
  - **修复**: 修复每日总结在插件重载时重复发送的问题，现在只有到达预定时间才会发送（首次更新插件依然会推送）。

- **v1.10.1**:
  - **优化**: 增强日志系统，解决 setup_logging 重复调用导致的 handler 重复添加问题。
  - **优化**: 增强 HTTP 客户端健壮性，添加连接池限制和请求信号量，防止高并发导致的问题。
  - **优化**: 增强主循环稳定性，添加指数退避策略，当连续错误发生时自动延长等待时间，避免频繁重试导致资源耗尽。
  - **修复**: 修复每日总结跨日边界问题，确保第二天能够正常触发推送。

- **v1.10.0**:
  - **新功能**: 增加运行日志功能 (plugin.log)，支持滚动记录和自定义文件大小，方便排查问题。
  - **新功能**: 增加每日推送记录功能，以日期命名 (如 20260316.log)，记录微博名、内容和链接，采用 JSON 格式，方便后续统计和分析。
  - **新功能**: 增加每日总结推送功能，每天 08:00 (可配置) 自动统计并推送前一日监控到的微博动态数量。
  - **优化**: 全面升级日志系统，提供更详尽的运行状态记录和错误堆栈追踪，方便定位复杂问题。

- **v1.9.0**:
  - **新功能**: 增加关键词白名单功能（whitelist_keywords），只有微博正文包含白名单关键词时才推送。（[#3](https://github.com/jiantoucn/astrbot_plugin_weibo_monitor/issues/3)）
  - **优化**: 完善过滤规则逻辑，先检查屏蔽词（黑名单），后检查白名单，确保用户体验流畅。

- **v1.8.1**:
  - **优化**: 允许插件在未配置 Cookie 时启动，但会跳过监控循环并输出警告日志，方便用户进入后台进行配置。

- **v1.8.0**:
  - **重大变更**: Cookie 改为必填项，未配置 Cookie 时插件将无法启动，确保数据抓取稳定性。
  - **启动检查增强**: 插件初始化时自动验证 Cookie 配置，未配置时抛出明确错误提示。
  - **文档**: 完善 README.md 文档，补充所有配置项说明、Cookie 获取教程和常见问题解答。
  - **文档**: 详细说明 `weibo_urls` 支持的三种格式（UID、个人主页URL、用户名URL）。
  - **文档**: 新增常见问题 FAQ 部分，解答用户使用过程中可能遇到的问题。

- **v1.7.7**:
  - **优化**: 对全代码进行深度审查和优化，确保在极端网络环境、数据损坏等场景下都能稳定运行。
  - **优化**: 为 HTTP 客户端添加自动重试机制（最多重试2次），提升网络不稳定时的请求成功率。
  - **优化**: 采用原子写入机制保存数据，避免程序意外中断导致数据文件损坏；增加数据损坏自动备份功能，防止数据丢失。
  - **优化**: 对 JSON 解析、数据结构校验、URL 解析等关键路径添加更完善的异常捕获和处理逻辑。
  - **优化**: 增加对各种异常数据结构的校验，避免因接口返回格式变化导致程序崩溃。

- **v1.7.6**:
  - **优化**: 精细化微博监控初始化逻辑，能更准确地在日志中区分"首次安装初始化"与"日常启动会话同步"。
  - **优化**: 优化了随机抖动 (Jitter) 的计算逻辑，确保在极端配置下仍能稳定运行，并对关键监控周期进行了深度自审。
  - **优化**: 改进了监控周期内的调试日志，方便用户更直观地了解下一次检查的预计时间。

- **v1.7.5**:
  - **新功能**: 为"检查间隔"和"账号请求间隔"引入了随机浮动参数，模拟真人无规律操作，提高安全性。
  - **新配置项**: `check_interval_jitter`: 检查间隔随机浮动（分钟）；`request_interval_jitter`: 请求间隔随机浮动（秒）。

- **v1.7.4**:
  - **新功能**: 增加区分原创和转发微博的功能。
  - **新配置项**: `send_original`: 是否推送原创微博 (默认开启)；`send_forward`: 是否推送转发微博 (默认开启)。

- **v1.7.3**:
  - **优化**: 彻底移除正文超链接：现在正文中的所有超链接（包括 Hashtag、提到的人、网页链接等）都将被移除超链接格式，仅保留文本内容，彻底解决消息观感不佳的问题。

- **v1.7.2**:
  - **优化**: 移除微博正文中 Hashtag (话题) 的超链接格式，仅保留文本内容，提升阅读观感。
  - **指令调整**: `/weibo_check` 调整为仅抓取监控列表中的第一个账号，方便快速单点测试；`/weibo_check_all` 新增指令，保留原有功能，抓取列表中所有账号的最新动态。

- **v1.7.1**:
  - **修复**: 修复了重构后可能导致推送目标会话 ID 只能推送一个的问题。
  - **优化**: 为每个推送目标添加独立的异常捕获，确保单个目标推送失败（如权限不足或网络问题）不会影响其他目标的推送。
  - **优化**: 增强了 `target_conversation_id` 和 `weibo_urls` 的解析逻辑，支持在列表项中混合使用逗号分隔的字符串，提升配置灵活性。
  - **优化**: 在推送成功后提供更详细的日志信息，包含成功推送的目标数量。

- **v1.7.0**:
  - **重构**: 遵循单一职责原则对代码进行深度重构，将复杂长函数拆分为多个职责单一的小函数，大幅提升可维护性。
  - **优化**: 所有硬编码值提取为统一常量，集中管理 URL、超时时间、默认配置等参数，便于后续维护。
  - **优化**: 为所有函数添加完整的类型注解，增强代码健壮性和 IDE 智能提示支持。
  - **优化**: 简化置顶判断、屏蔽词检查等核心逻辑，使用 `any()` 替代多重 `or` 判断，提升运行效率。
  - **优化**: 统一 URL 解析逻辑，消除多处重复的 URL 处理代码，减少冗余。
  - **优化**: 完善边界情况检查，添加空文本、类型校验等防御性代码，避免潜在崩溃点。
  - **优化**: 统一日志格式，添加更详细的调试信息，方便问题排查和运行状态监控。
  - **优化**: 所有关键路径添加异常捕获，确保单个 URL 检查失败不会影响整个监控任务运行。
  - **文档**: 为所有公共和私有方法添加清晰的文档字符串，说明功能、参数和返回值。
  - **优化**: 统一变量和函数命名规范，优化代码缩进和空行使用，提升代码美观度和可读性。

- **v1.6.0**:
  - **新功能**: 增加配置导出与导入功能。使用 `/weibo_export` 即可将当前插件的所有配置导出为 Base64 字符串；使用 `/weibo_import [配置字符串]` 即可快速恢复或迁移配置。
  - **兼容性设计**: 导入逻辑采用键值对合并模式，确保了跨版本导入的兼容性。即使在未来版本中增加新配置项，导入旧版本的配置字符串也不会导致插件崩溃或新功能失效。

- **v1.5.2**:
  - **重构**: 遵循单一职责原则 (SRP) 对 `check_weibo` 方法进行了深度重构，将网络请求、博文提取、过滤逻辑拆分为独立方法，大幅提升代码可维护性。
  - **修复**: 修复了当最新微博全部被过滤时导致 `last_id` 更新停滞的严重漏洞。现在无论是否被过滤，只要获取到更新的 ID 均会同步状态，避免重复比对。
  - **优化**: 优化了配置读取逻辑，将消息格式化模板封装为属性，避免在监控循环中进行重复的字符串替换操作。
  - **优化**: 引入防御性编程，优化了多处链式字典取值逻辑（如 `(mblog.get("title") or {}).get("text")`），彻底消除 `NoneType` 导致的潜在崩溃点。

- **v1.5.1**:
  - **测试**: 完成全量功能自查与集成测试，确保置顶过滤、屏蔽词过滤、启动静默、数据迁移等功能在生产环境下稳定运行。

- **v1.5.0**:
  - **新功能**: 新增"屏蔽词"功能：支持在插件设置中配置屏蔽词列表。若微博正文包含任何屏蔽词，该条微博将不会被推送。

- **v1.4.14**:
  - **优化**: 全量代码自查与鲁棒性增强：将网络请求超时从 10s 提升至 20s，降低网络波动导致的抓取失败率。
  - **优化**: 优化内部日志逻辑，确保生产环境下运行稳定。

- **v1.4.13**:
  - **优化**: 引入"会话静默初始化"机制：每次插件启动或重载后的第一次检查仅同步各账号的最新 ID，不再推送离线期间积累的历史动态，避免"启动即炸群"。

- **v1.4.12**:
  - **修复**: 增强置顶微博过滤逻辑，支持多种置顶标识识别（isTop, is_top, title等），解决部分账号一直获取到置顶微博的问题。
  - **优化**: 支持一次性推送多个最新动态（按时间顺序从旧到新），不再只推送最新一条。

- **v1.4.11**:
  - **修复**: 修复热重载后后台监控任务无法自动启动的问题。
  - **优化**: 增加数据目录自动迁移逻辑，兼容旧路径数据，防止升级后首轮推送跳过。

- **v1.4.10**:
  - **重构**: 代码深度优化与自查修复：统一使用 `pathlib.Path` 方法处理数据加载与保存。
  - **优化**: 增强后台监控任务的异常捕获，单个账号失败不再影响其他账号。
  - **修复**: 优化 `on_loaded` 钩子，防止重复创建监控任务。

- **v1.4.9**:
  - **重构**: 遵循 AstrBot 插件开发最佳实践进行优化：使用 `StarTools.get_data_dir()` 规范化数据存储路径。
  - **优化**: 将后台监控任务的启动移至 `@filter.on_astrbot_loaded()` 钩子中。
  - **优化**: 增强 `clean_text` 方法的类型安全性。

- **v1.4.8**:
  - **修复**: 修正 `metadata.yaml` 中的 `name`、`desc` 和 `repo` 字段以符合验证要求。

- **v1.4.7**:
  - **配置**: 配置 `repo_url` 以支持 AstrBot 强制从仓库地址更新的功能。

- **v1.4.6**:
  - **文档**: 添加文档按钮链接。

- **v1.4.2**:
  - **修复**: 修复 `/weibo_verify` 指令在某些情况下可能出现的 `KeyError: 'user'` 崩溃问题。
  - **优化**: 增强 Cookie 验证逻辑的健壮性。

- **v1.4.1**:
  - **新配置项**: 新增 `request_interval` 配置项，支持设置多个账号抓取之间的延迟时间（默认 5s），提高稳定性。

- **v1.4.0**:
  - **新功能**: 支持向多个推送目标会话推送动态。
  - **优化**: 优化 `target_conversation_id` 配置项为列表类型。

- **v1.3.1**:
  - **修复**: 修复自定义消息格式不支持换行的问题（现在可以使用 `\n` 进行换行）。

- **v1.3.0**:
  - **新功能**: 新增 `/weibo_verify` 指令，验证 Cookie 是否有效。
  - **新功能**: 支持自定义消息格式，提供 `{name}`、`{weibo}`、`{link}` 变量。
  - **修复**: 优化微博删除后的逻辑，采用 ID 数值比较防止重复推送。

- **v1.2.0**:
  - **新功能**: 新增 `/weibo_check` 指令，支持立即执行监控检查并发送最新微博（用于测试）。

- **v1.1.2**:
  - **修复**: 修复 `WeiboMonitor object has no attribute 'config'` 运行时错误。

- **v1.1.1**:
  - **修复**: 修复 WebUI 设置页面显示为空的问题。
  - **优化**: 优化配置项定义，支持列表类型。

- **v1.1.0**:
  - **新配置项**: 增加 Cookie 配置项以增强稳定性。
  - **优化**: 优化请求头，模拟移动端访问。
  - **新功能**: 增加 `/get_umo` 指令方便获取推送目标。
